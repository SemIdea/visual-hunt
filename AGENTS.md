# AGENTS.md

This project uses AFM (Agent Flow Methodology).

- Canonical guidance: see `CLAUDE.md` (doc map + doctrine).
- Hard rules with executable triggers: `docs/afm.md` § 3.
- Architecture / where code goes: `docs/ach.md` § 4.
- Decision rubrics (SOLID, error classification, when to create lib/module/DSL, validation boundary): `docs/rubrics/`.
- Known pitfalls: `docs/gotchas.md`.

Rules live in `docs/` (single source) — this file only points to them; do not duplicate rules here.
